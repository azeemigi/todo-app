# todo-app

## Run the app

From the repository root, start the API with Java 25:

```bash
JAVA_HOME=/home/codespace/java/25.0.2-ms PATH=/home/codespace/java/25.0.2-ms/bin:$PATH mvn spring-boot:run -pl todo-api -am
```
